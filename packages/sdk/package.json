{
  "name": "@pillar/sdk",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsup index.ts core.ts --format cjs --dts",
    "dev": "npm run build -- --watch"
  },
  "dependencies": {
    "@pillar/core": "workspace:*"
  },
  "devDependencies": {
    "eslint": "^8.33.0",
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*",
    "tsup": "^6.5.0",
    "typescript": "^4.9.5"
  },
  "version": "0.2.0"
}
