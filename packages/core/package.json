{
  "name": "@pillar/core",
  "private": false,
  "version": "0.1.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "ISC",
  "scripts": {
    "lint": "eslint *.ts*",
    "build": "rm -rf dist && tsup index.ts --format cjs --dts",
    "dev": "npm run build -- --watch"
  },
  "devDependencies": {
    "@types/react": "^18.0.17",
    "@types/react-dom": "^18.0.6",
    "eslint": "^8.33.0",
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*",
    "tsup": "^6.5.0",
    "typescript": "^4.9.5"
  }
}
